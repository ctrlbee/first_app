<%= form_for(@microcomment) do |f| %>
  <% if @microcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microcomment.errors.count, "error") %> prohibited this microcomment from being saved:</h2>

      <ul>
      <% @microcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
